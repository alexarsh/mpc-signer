node:
  id: "s2"
  port: 18552

peer:
  host: "localhost"
  port: 19552
  role: "client"  # S2 connects to S1's WebSocket

api:
  host: "0.0.0.0"
  port: 18552

keystore:
  dir: "./data/s2/keystore"
  passphrase: "poc-only-change-in-prod"

tron:
  network: "nile"
  fullnode: "https://nile.trongrid.io"
