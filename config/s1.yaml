node:
  id: "s1"
  port: 18551

peer:
  host: "localhost"
  port: 19552
  role: "server"  # S1 is WebSocket server

api:
  host: "0.0.0.0"
  port: 18551

keystore:
  dir: "./data/s1/keystore"
  passphrase: "poc-only-change-in-prod"

tron:
  network: "nile"
  fullnode: "https://nile.trongrid.io"
