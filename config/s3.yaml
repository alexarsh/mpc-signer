node:
  id: "s3"
  port: 18553

peer:
  host: "localhost"
  port: 19552
  role: "client"  # S3 connects to S1's WebSocket

api:
  host: "0.0.0.0"
  port: 18553

keystore:
  dir: "./data/s3/keystore"
  passphrase: "poc-only-change-in-prod"

tron:
  network: "nile"
  fullnode: "https://nile.trongrid.io"
